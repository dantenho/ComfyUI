# ComfyUI Requirements
# Python 3.14+ compatible
# CUDA 13.0/13.1 compatible (requires driver >= 580, compute capability >= 7.5)

comfyui-frontend-package==1.35.9
comfyui-workflow-templates==0.7.65
comfyui-embedded-docs==0.3.1

# PyTorch - CUDA 13.x support
# Note: CUDA 13.x requires specific PyTorch builds with cu13 suffix
# Python packages: nvidia-cuda-* (not nvidia-cuda-*-cu12) with layout at site-packages/nvidia/cu13/
torch
torchsde
torchvision
torchaudio

numpy>=2.4.0
einops
transformers>=4.50.3
tokenizers>=0.13.3
sentencepiece
safetensors>=0.4.2
aiohttp>=3.11.8
yarl>=1.18.0
pyyaml
Pillow
scipy
tqdm
psutil
alembic
SQLAlchemy
av>=14.2.0

#non essential dependencies:
kornia>=0.7.1
spandrel
pydantic~=2.0
pydantic-settings~=2.0
